# Server Configuration
PORT=5000
NODE_ENV=development

# Database - MongoDB Atlas (Cloud Database)
# Replace <username>, <password>, and <cluster-url> with your MongoDB Atlas credentials
# Get your connection string from: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/meri-shikayat?retryWrites=true&w=majority

# For local MongoDB (if you install it later), use:
# MONGODB_URI=mongodb://localhost:27017/meri-shikayat

# JWT Secret - CRITICAL SECURITY SETTING
# ⚠️ IMPORTANT: You MUST generate a secure random string for production!
# 
# Generate a secure 64+ character secret using this command:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# Requirements:
# - Minimum 64 characters
# - Cryptographically random
# - Different for each environment (dev, UAT, production)
# - NEVER commit the actual secret to git
# - Store in environment variables or secrets manager (AWS Secrets Manager, Azure Key Vault)
#
# Example (DO NOT USE THIS - generate your own):
# JWT_SECRET=7a6445093ffd34a37c642188dcbd0640a605ecbaa073ecbe5af14b46c0f000c243151776af9cad2b81337d5c309e640205662f0079f27996372b2f4a4d7ceb5e
JWT_SECRET=REPLACE_THIS_WITH_YOUR_SECURE_64_CHAR_SECRET
JWT_EXPIRE=7d

# For zero-downtime secret rotation (optional):
# JWT_SECRET_PREVIOUS=your_previous_secret_here

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# CORS - Allowed Origins (comma-separated for multiple origins)
# Production example: CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# Logging
LOG_LEVEL=debug

# Cloud Storage (Optional)
CLOUD_STORAGE_BUCKET=
CLOUD_STORAGE_KEY=
CLOUD_STORAGE_SECRET=

# Redis Configuration (for Rate Limiting and Caching)
# Local development: Use default localhost settings
# Production: Use Redis Cloud or managed Redis service
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# For Redis Cloud (production):
# REDIS_HOST=redis-12345.c1.us-east-1-2.ec2.cloud.redislabs.com
# REDIS_PORT=12345
# REDIS_PASSWORD=your_redis_password_here

# SMS Configuration (Fast2SMS)
# Get API key from: https://www.fast2sms.com/
FAST2SMS_API_KEY=your_fast2sms_api_key_here
FAST2SMS_SENDER_ID=MSHKYT

# Email Configuration (Gmail)
# Use App Password for Gmail: https://support.google.com/accounts/answer/185833
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# OTP Configuration
OTP_EXPIRY_MINUTES=5
MAX_OTP_ATTEMPTS=5
OTP_RATE_LIMIT_MINUTES=2

# ML Service Configuration
ML_SERVICE_URL=http://localhost:8000
ML_SERVICE_ENABLED=true
